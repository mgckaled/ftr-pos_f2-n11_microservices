# JWT Keys (gerar usando: node -e "const crypto = require('crypto'); const {privateKey, publicKey} = crypto.generateKeyPairSync('rsa', {modulusLength: 2048, publicKeyEncoding: {type: 'spki', format: 'pem'}, privateKeyEncoding: {type: 'pkcs8', format: 'pem'}}); console.log('PRIVATE:', privateKey); console.log('PUBLIC:', publicKey);")
JWT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nGERAR_CHAVE_AQUI\n-----END PRIVATE KEY-----"
JWT_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\nGERAR_CHAVE_AQUI\n-----END PUBLIC KEY-----"

# Database URLs
AUTH_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/auth
ORDERS_DATABASE_URL=postgresql://postgres:postgres@localhost:5433/orders
INVENTORY_DATABASE_URL=postgresql://postgres:postgres@localhost:5434/inventory
ANALYTICS_DATABASE_URL=postgresql://postgres:postgres@localhost:5435/analytics

# Kafka
KAFKA_BROKERS=localhost:9092

# Redis
REDIS_URL=redis://localhost:6379

# Services URLs
AUTH_SERVICE_URL=http://localhost:3000
ORDERS_SERVICE_URL=http://localhost:3001
INVENTORY_SERVICE_URL=http://localhost:3002
ANALYTICS_SERVICE_URL=http://localhost:3003

# JWKS
AUTH_JWKS_URI=http://auth-service:3000/.well-known/jwks.json
