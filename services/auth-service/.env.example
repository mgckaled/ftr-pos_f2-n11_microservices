PORT=3000

# NODE_ENV options:
# - development (default): connects to localhost
# - docker: connects to Docker container names
NODE_ENV=development

# DATABASE_URL (optional, auto-generated based on NODE_ENV if not set)
# For local development: postgresql://postgres:postgres@localhost:5432/auth
# For Docker: postgresql://postgres:postgres@auth-db:5432/auth
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/auth

# Gerar chaves RSA:
# node -e "const crypto = require('crypto'); const {privateKey, publicKey} = crypto.generateKeyPairSync('rsa', {modulusLength: 2048, publicKeyEncoding: {type: 'spki', format: 'pem'}, privateKeyEncoding: {type: 'pkcs8', format: 'pem'}}); console.log('PRIVATE KEY:'); console.log(privateKey); console.log('\nPUBLIC KEY:'); console.log(publicKey);"

JWT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----
...
-----END PRIVATE KEY-----"

JWT_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----
...
-----END PUBLIC KEY-----"
