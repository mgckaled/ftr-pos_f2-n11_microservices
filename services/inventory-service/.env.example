PORT=3002

# NODE_ENV options:
# - development (default): connects to localhost
# - docker: connects to Docker container names
NODE_ENV=development

# DATABASE_URL (optional, auto-generated based on NODE_ENV if not set)
# For local development: postgresql://postgres:postgres@localhost:5434/inventory
# For Docker: postgresql://postgres:postgres@inventory-db:5432/inventory
# DATABASE_URL=postgresql://postgres:postgres@localhost:5434/inventory

KAFKA_BROKERS=localhost:9092
REDIS_URL=redis://localhost:6379

AUTH_JWKS_URI=http://localhost:3000/.well-known/jwks.json
